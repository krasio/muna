<% if flash[:alert] %>
  <div class="alert"><%= flash[:alert] %></div>
<% end %>

<%= form_for @secret, url: {action: "create"} do |f| %>
  <%= f.text_area :raw_content %>

  <%= f.submit "Keep it secret. Keep it safe." %>
<% end %>
